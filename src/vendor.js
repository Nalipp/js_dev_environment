/*
This file contains references to the user libraries we are using in the project. This is used by webpack in the production build only*
This prevents users from having to download a huge JS file anythime a small change is made. Any files that aren't referenced here will be bundled into the main.js for the production build.
*/

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch'

/*
import jQuery
import react
import bootstrap
...
*/
